import pandas as pd\nimport numpy as np\nimport random\n\n# Constants\nNUM_TRANSACTIONS = 5000\nNUM_CUSTOMERS = 200\nNUM_PRODUCTS = 16\nSTORE_ID = 'S001'\nCITY = 'Coimbatore'\nDATE_RANGE_START = '2024-01-01'\nDATE_RANGE_END = '2026-03-31'\n\n# Sample Data\nproduct_names = [f'Product_{i+1}' for i in range(NUM_PRODUCTS)]\ncategories = ['Fruits', 'Vegetables', 'Dairy', 'Beverages', 'Snacks', 'Bakery', 'Frozen Foods', 'Canned Goods']\npayment_methods = ['Credit Card', 'Debit Card', 'Cash', 'Mobile Wallet']\n\n# Generate Data\ndef generate_data():\n    transaction_id = []\n    customer_id = []\n    dates = []\n    store_id = []\n    product_id = []\n    product_name = []\n    category = []\n    quantity = []\n    unit_price = []\n    total_amount = []\n    payment_method = []\n    city = []\n    customer_age = []\n    customer_gender = []\n\n    for _ in range(NUM_TRANSACTIONS):\n        transaction_id.append(_)\n        customer_id.append(random.randint(1, NUM_CUSTOMERS))\n        dates.append(pd.to_datetime(random.choice(pd.date_range(DATE_RANGE_START, DATE_RANGE_END))))\n        store_id.append(STORE_ID)\n        product_index = random.randint(0, NUM_PRODUCTS - 1)\n        product_id.append(product_index + 1)\n        product_name.append(product_names[product_index])\n        category.append(random.choice(categories))\n        qty = random.randint(1, 10)\n        quantity.append(qty)\n        price = round(random.uniform(1.0, 50.0), 2)\n        unit_price.append(price)\n        total_amount.append(round(qty * price, 2))\n        payment_method.append(random.choice(payment_methods))\n        city.append(CITY)\n        customer_age.append(random.randint(18, 65))\n        customer_gender.append(random.choice(['Male', 'Female']))\n\n    return pd.DataFrame({\n        'Transaction_ID': transaction_id,\n        'Customer_ID': customer_id,\n        'Date': dates,\n        'Store_ID': store_id,\n        'Product_ID': product_id,\n        'Product_Name': product_name,\n        'Category': category,\n        'Quantity': quantity,\n        'Unit_Price': unit_price,\n        'Total_Amount': total_amount,\n        'Payment_Method': payment_method,\n        'City': city,\n        'Customer_Age': customer_age,\n        'Customer_Gender': customer_gender\n    })\n\nif __name__ == '__main__':\n    df = generate_data()\n    df.to_csv('grocery_customer_data.csv', index=False)\n    print('Generated grocery customer purchase data.')\n