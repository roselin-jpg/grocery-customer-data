import pandas as pd\nimport random\nfrom datetime import datetime, timedelta\n\n# Constants\nNUM_ROWS = 5000\nNUM_PRODUCTS = 14\nSTORE_ID = 'S001'\nCITY = 'Coimbatore'\n\n# Sample product names\nproduct_names = [f'Product_{i+1}' for i in range(NUM_PRODUCTS)]\n\n# Generate random customer data\ncustomer_data = []\nfor i in range(NUM_ROWS):\n    customer_id = f'CUST_{i+1}'\n    purchase_date = datetime.utcnow() - timedelta(days=random.randint(0, 30))  # within the last 30 days\n    item_purchased = random.sample(product_names, k=random.randint(1, NUM_PRODUCTS))\n    for product in item_purchased:\n        customer_data.append({\n            'customer_id': customer_id,\n            'purchase_date': purchase_date.strftime('%Y-%m-%d %H:%M:%S'),\n            'store_id': STORE_ID,\n            'city': CITY,\n            'product': product\n        })\n\n# Create DataFrame\ndf = pd.DataFrame(customer_data)\n\n# Save to CSV\ndf.to_csv('grocery_customer_data.csv', index=False)\nprint('CSV file generated with 5000 rows of grocery customer data.')\n